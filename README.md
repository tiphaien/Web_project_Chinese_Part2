# Mandarin Learning Backend API


This backend project is a Node.js + Express server that provides API endpoints for navigating a list of Mandarin-learning topics. The server supports browsing items one by one using next/previous functionality.


## 📁 Project Structure

## Files


- `data/items.js` — array of objects (title, desc, img)
- `server.js` — Express server and API endpoints
- `package.json` — project manifest
- `.gitignore`


## Setup


1. Clone or copy this `backend` folder.
2. From the `backend` folder, run:

## To do in the terminal to see the result on localhost:3000...
npm run dev
